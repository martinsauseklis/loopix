# @loopix/core

The framework-agnostic contract for [loopix](../../README.md): the report
schema/types, the `ReportStore` interface + `FileReportStore`, and the
config/flags + service-registry types.

See the [loopix README](../../README.md) for usage.
