# @loopix/nextjs

The loopix host SDK for Next.js (App Router): capture UI (`LoopixProvider`),
the operator `LoopixDashboard`, server route factories, and the proxy gate
(`@loopix/nextjs/server`).

See the [loopix README](../../README.md) for the full integration guide.
